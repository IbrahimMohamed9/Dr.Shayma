import SubjectTemplate from "../templates/SubjectTemplate";
import { HEALTH } from "../../assets/utils/Constants";

const Health = () => {
  return <SubjectTemplate title={HEALTH} category={HEALTH} />;
};

export default Health;
